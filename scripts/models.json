{
  "_comment": "Free-tier Gemini model pool. Each model has its OWN daily quota, so the pipeline drains them in order of preference per role. rpm/rpd are soft caps: if Google returns a real daily-quota 429 first, the model is parked until the Pacific-midnight reset; if the cap is lower than the real quota, raise it here. Update these numbers from the AI Studio rate-limit page for your key.",
  "quota_reset_timezone": "America/Los_Angeles",
  "reserve_for_ingestion": 40,
  "models": [
    {
      "name": "gemini-3.5-flash",
      "rpm": 10,
      "rpd": 250,
      "json_mode": true,
      "roles": ["outline", "draft"]
    },
    {
      "name": "gemini-3.1-flash-lite",
      "rpm": 15,
      "rpd": 1000,
      "json_mode": true,
      "roles": ["classify", "draft", "outline"]
    },
    {
      "name": "gemini-3.5-flash-lite",
      "rpm": 15,
      "rpd": 1000,
      "json_mode": true,
      "roles": ["classify", "draft", "outline"]
    },
    {
      "name": "gemma-3-27b-it",
      "rpm": 30,
      "rpd": 14400,
      "json_mode": false,
      "roles": ["classify", "draft"]
    }
  ]
}
